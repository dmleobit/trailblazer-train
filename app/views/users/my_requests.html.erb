<h1>My requests</h1>

<% @records.each do |record| %>
  <div class='user-block' id='record-<%= record.id %>'>
    <%= record.receiver.email %>
    <%= link_to  "Cancel", cancel_friendship_path(record.id), method: :delete, class: 'btn btn-outline-danger' %>
  </div>
<% end %>
